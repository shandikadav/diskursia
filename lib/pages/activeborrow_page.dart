import 'package:flutter/material.dart';

class ActiveBorrow extends StatelessWidget {
  const ActiveBorrow({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Ini Page Peminjaman Aktif"),
      ),
    );
  }
}